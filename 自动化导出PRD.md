# 试卷自动化排版导出功能 PRD (V1.0)

## 1. 背景与目标
在现有“零散任务处理”的基础上，打造一个**“试卷级排版与导出工作台”**，帮助使用者将经过 AI 解析后的单体题目，还原并组装为格式标准、自动标号、可以实际投入生产和打印的整套试卷。

## 2. 界面层级与导航结构 (UI/UX)
将前端视图层级进行重构，从当前的 `[看板] + [后台管理弹窗]` 升级为平级的顶层导航结构。
顶层至少包含三大核心视图，通过系统全局的导航栏（比如左侧导航栏或顶栏）进行平滑切换：
1. **工作台 (Task Dashboard)**：用于快速粘贴图片提交题目任务、实时进度监控。
2. **数据库 (Admin Panel)**：原后台面板，用于全局检索任务、人工接管、校对排版源码、干预大模型逻辑。
3. **排版台 (Paper Builder)**（本次核心新增！）：专门用于将多道题库中的任务组合为成标准试卷。

## 3. 试卷排版视图 (Paper Builder) 功能详述
排版视图采用**左右双栏拖拽布局（或网格布局）**：

### 3.1 左侧：任务池 (Task Pool)
* **内容承载**：呈现和“数据库视图”高度一致的任务卡片列表，且能够直观展示每道题的状态。单张卡片需包含：任务缩略图、任务状态指示灯（queued/solving/completed/failed）、题干前缀预览。
* **状态过滤**：支持快速筛选就绪（`completed`）可以被排版的试题。
* **快捷跳转联通**：在排版卡片上提供一个**“详情/编辑”**的快捷入口。点击后：
  * 系统自动将当前视图切换到**数据库视图 (Admin Panel)**。
  * 自动选中该道题的 `task_id` 并展开它的历史记录和排版数据（`draft_solution`/`final_result`）。
  * 设计目的是让使用者如果在排版编排时发现题目小瑕疵，能无缝过去改完再切回来。

### 3.2 右侧：整卷构建工作台 (Builder Console)
* **题型区块 (Group) 管理**：
  * 提供快捷按钮：“+ 单项选择题”、“+ 填空题”、“+ 计算解答题”、“+ 综合题”等。
  * **题型重命名**：允许用户点击题型区块标题进行重命名（例如手动改为“现代文阅读”或“二、非客观题”），自定义度极高。
  * **区块级别拖拉拽**：支持上下拖拽整个题型区块，以调整大模块顺序（例如把判断题挪到大题后面）。
* **题目级别嵌套拖拽 (Nested DnD)**：
  * 使用者可从左侧任务池把卡片直接拖入右侧对应的“题型区块”内。
  * 在同一区块内，支持题目上下拖动，调整在该题型内的出题顺序（第 1/2/3 题）。
  * 允许题目跨组拖拽（比如原本发到填空块的题目移动到选择块）。

## 4. 后端排版与渲染逻辑

### 4.1 数据通信契约变更 (API DTO)
现有的导出接口接受的是扁平的 `task_ids: string[]`。须通过二维数据结构传送组卷架构：
```json
{
  "groups": [
    {
      "group_name": "选择题",
      "task_ids": ["task-1", "task-2"]
    },
    {
      "group_name": "计算题",
      "task_ids": ["task-3"]
    }
  ]
}
```

### 4.2 全自动序号生成引擎
在目前的 `build_split_export_markdown` 基础上重做渲染引擎，取代目前单纯的字符串拼接。由后端根据该 JSON 完全接管题号生成，确保绝对干净：
1. **题型（外层）序号映射**：根据后端接收到 groups 的顺序（index），动态赋予“一、”“二、”“三、”题型头。最终拼成：`# 一、选择题`。
2. **题目（内层）序号挂载**：在每个题型块内遍历时，顺序下发 `1.` `2.` `3.` 序号前缀。
3. **LLM 废序号静默清洗**：由于大模型有时候可能自带“（1）已知...”、“1. ...”，需要加一层正则处理将其清理掉，以防串号出现 `1. 1. 已知`。

### 4.3 终版交付结构：“二段式双面卷”
为满足教学工作流需求，“题”“解”要彻底物理隔离。由于我们在之前已经确保 LLM 输出 `【正解】`/`【解析】` 标记，导出的最终文件将会自动按题型架构产生两个独立章节区：
* **【第一部分：原卷试题版】**：
  一、选择题
  1. (题干内容A)
  2. (题干内容B)
  二、解答题
  1. (题干内容C)
* **【第二部分：答案与解析版】**：
  一、选择题
  1. 【正解】...【解析】...
  2. 【正解】...
  二、解答题
  1. 【解析】...

## 5. 开发建议实施路径
1. **Phase 1: 数据层与后端生成算法升级 (Backend)**  
   - 修改 `schemas.py` 扩充二维数组导出支持。
   - 重构 `main.py` 的 markdown/docx 导出渲染流水线。
2. **Phase 2: 导航与多视图融合基建 (Frontend)**  
   - 修改 `App.tsx` 页面路由逻辑，引入三个并行状态 `activeView: 'dashboard' | 'admin' | 'builder'`。并传递全局的跳转状态。
3. **Phase 3: 双栏排版台与嵌套 DnD 实现 (Frontend)**  
   - 使用成熟拖拽包（如 `dnd-kit` ）落地复杂的二维拖拽、区块增删改交互。

## 6. 架构可实现性评审与开发准入

### 6.1 可实现性结论
- 结论：**当前架构可实现本 PRD，建议进入开发**。
- 理由：后端已具备导出 API 与任务聚合能力，前端已具备任务检索与状态展示能力，仅需在“导出契约、渲染器、视图编排”三处升级。

### 6.2 当前代码差距（Gap）
1. 前端当前是双视图（工作台/数据库），尚未实现“排版台”并列入口。
2. 导出请求体仍为扁平 `task_ids`，尚不支持 `groups` 分组结构。
3. 导出 markdown 仍是平铺拼接，尚未实现“题型中文序号 + 组内阿拉伯序号”。
4. 视图间缺少“从排版台跳转数据库并定位 task”的共享状态机制。

### 6.3 技术选型评估
- 前端拖拽：推荐 `dnd-kit`（支持 nested sortable，维护活跃，适合本项目复杂拖拽）。
- 后端渲染：继续沿用 FastAPI + Pydantic，新增分组 DTO 与编号渲染器即可。
- 文档导出：继续沿用 Markdown -> Pandoc -> DOCX 链路，无需替换。

### 6.4 API 与数据契约（落地版）
- 请求兼容策略：导出接口支持“双轨输入”。
  - 旧版：`task_ids: string[]`
  - 新版：`groups: [{ group_id, group_name, task_ids[] }]`
- 服务端优先级：若传入 `groups`，按 `groups` 导出；否则回退旧逻辑。
- 建议约束：
  - `group_id` 必填且全局唯一（前端生成 UUID）。
  - `group_name` 允许自定义，但导出时统一渲染为 `一、{group_name}`。
  - `task_ids` 去重，保留首次出现顺序。

### 6.5 开发准入检查清单（DoR）
1. 前端安装并启用拖拽依赖（`dnd-kit`）。
2. 后端完成 schema 扩展并通过基础请求校验。
3. 导出渲染器支持编号并完成空组/空题目保护。
4. 前端完成三视图导航与跨视图 task 定位参数传递。
5. 回归验证：旧版 `task_ids` 导出链路不回归。

### 6.6 验收补充（面向开发联调）
1. 在排版台创建 3 个题型并任意排序后，导出顺序与界面一致。
2. 同一题型内拖拽排序后，导出题号 `1/2/3` 与界面一致。
3. 点击排版台任务“去数据库”，可跳转并自动选中该任务。
4. 导出 MD 与 DOCX 均包含“原卷试题版 + 答案与解析版”两段结构。
5. 旧前端仅传 `task_ids` 仍可导出，功能不报错。
