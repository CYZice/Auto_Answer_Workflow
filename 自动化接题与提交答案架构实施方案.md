# 自动化接题与提交答案架构实施方案（可执行拆解）

## 输入增强
### 1) PM PRD 文档
- d:/Microsoft VS Code/Auto_Answer_Workflow/自动化接题与提交答案需求澄清.md

### 2) 当前项目文件树（裁剪版）
- backend/
  - requirements.txt
  - app/
    - main.py
    - api/
      - routes.py（当前为空）
    - core/
      - database.py
      - events.py
    - models/
      - domain.py
      - schemas.py
    - services/
      - runtime_config.py
    - agent/
      - graph.py
      - state.py
      - nodes/
        - solver.py
        - reviewer.py
        - formatter.py
- frontend/
  - package.json
  - src/
    - main.tsx
    - App.tsx（现有大一体页面）

### 3) 核心文件代码摘要（仅定义级）
- backend/app/main.py
  - FastAPI 主入口，含任务流执行与并发信号量逻辑。
  - 现有 API/工作流逻辑较重，不适合继续堆叠新业务。
- backend/app/models/domain.py
  - Task、AgentLog 两个 SQLAlchemy 模型；Task 状态为旧工作流状态集。
- backend/app/models/schemas.py
  - 现有请求/响应契约集中在旧工作流（queued/solving/reviewing/...）。
- backend/app/core/events.py
  - TaskEventBus（基于 asyncio.Queue 的任务事件总线）。
- backend/app/agent/*.py
  - 现有 LangGraph 解题链路，包含 solver/reviewer/formatter。
- frontend/src/App.tsx
  - 现有单体前端，功能复杂且与旧流程耦合较深。

---

## 架构原则（Minimal Invasive）
1. 新能力与旧能力隔离：新增模块，不改旧工作流核心节点。
2. 尽量不改大文件：避免把新业务继续堆到 backend/app/main.py 与 frontend/src/App.tsx。
3. 所有自动化流程走新命名空间：automation。
4. 严格串行：max_concurrency 固定为 1（业务硬约束）。
5. 失败可恢复：提交失败人工处理；解题失败继续下一题。

---

## 上下文剪裁（Context Pruning）
### Coder 必读文件（只读/实现）
- 必读并可改：
  - backend/requirements.txt
  - backend/app/main.py（仅注册新 router，不写业务实现）
  - backend/app/core/database.py（可复用，不改结构）
  - backend/app/core/events.py（可复用扩展任务流事件）
- 新增并实现：
  - backend/app/api/automation_routes.py
  - backend/app/automation/models.py
  - backend/app/automation/schemas.py
  - backend/app/automation/repository.py
  - backend/app/automation/service.py
  - backend/app/automation/browser_worker.py
  - backend/app/automation/markdown_renderer.py
  - backend/app/automation/state_machine.py
  - backend/app/automation/logging_store.py
- 前端（新控制台）新增：
  - frontend/src/automation-console/main.tsx
  - frontend/src/automation-console/App.tsx
  - frontend/src/automation-console/api.ts
  - frontend/src/automation-console/types.ts
  - frontend/src/automation-console/components/*

### Coder 禁止修改文件
- backend/app/agent/nodes/solver.py
- backend/app/agent/nodes/reviewer.py
- backend/app/agent/nodes/formatter.py
- backend/app/agent/graph.py
- backend/app/agent/state.py
- frontend/src/App.tsx

### Coder 可只读参考，不实现改动
- backend/app/models/domain.py
- backend/app/models/schemas.py
- backend/app/services/runtime_config.py

---

## 影响范围分析
1. 新增后端自动化子模块（主要影响）。
2. backend/app/main.py 仅做一行 include_router 级别接入。
3. backend/requirements.txt 增加自动化依赖。
4. 新增前端控制台目录，不侵入现有 frontend/src/App.tsx。
5. 数据库新增两张表，不改旧 tasks/agent_logs 表。

建议新增文件清单：
- backend/app/automation/__init__.py
- backend/app/automation/models.py
- backend/app/automation/schemas.py
- backend/app/automation/repository.py
- backend/app/automation/state_machine.py
- backend/app/automation/service.py
- backend/app/automation/browser_worker.py
- backend/app/automation/markdown_renderer.py
- backend/app/automation/logging_store.py
- backend/app/api/automation_routes.py
- frontend/src/automation-console/main.tsx
- frontend/src/automation-console/App.tsx
- frontend/src/automation-console/api.ts
- frontend/src/automation-console/types.ts
- frontend/src/automation-console/components/TaskTable.tsx
- frontend/src/automation-console/components/ReviewPanel.tsx
- frontend/src/automation-console/components/RunControls.tsx
- frontend/src/automation-console/components/LogPanel.tsx

---

## 依赖分析（Dependency Analysis）

### 现状
- Python：FastAPI、SQLAlchemy、httpx、langgraph 等已存在。
- Frontend：React、axios、react-markdown、remark-math、rehype-katex 已存在。

### 建议新增
1. backend/requirements.txt
- playwright==1.53.0
- python-multipart>=0.0.9
- markdown-it-py>=3.0.0

2. 运行时安装
- python -m playwright install chromium

### 兼容性评估
1. playwright 与 FastAPI/SQLAlchemy 无直接冲突，独立运行线程即可。
2. python-multipart 与现有 FastAPI 兼容。
3. markdown-it-py 为纯 Python 库，风险低。
4. 不引入 Celery/Redis，保持单进程串行，满足“一次一题”。

---

## 数据流设计（Data Flow Design）

### 1) 扫描阶段
1. 前端调用 POST /api/automation/scan/start。
2. service 驱动 browser_worker 遍历学校并抓取待解题。
3. repository upsert 到 automation_tasks（status=discovered）。
4. logging_store 追加 JSONL 事件。

### 2) 批量接单阶段
1. 前端提交 selected 任务。
2. service 按 school_name 分组，逐组串行接单。
3. 成功接单状态转 grabbed。

### 3) 解题与写入阶段
1. service 从 grabbed 队列取 1 题。
2. browser_worker 抓题图 URL。
3. 调用现有工作流（读取 final_answer/final_result）。
4. state_machine：
   - 工作流失败 -> solve_failed（继续下一题）
   - 成功 -> markdown_renderer 生成解析图片
5. browser_worker 触发“识别录入”并等待 imgToText 成功信号。
6. 解析写入 + 考点衍生文本写入。
7. 状态转 review_pending。

### 4) 复核与提交阶段
1. 前端展示 review_pending 内容，用户可编辑。
2. 保存编辑后状态 ready_to_submit。
3. 用户确认提交 -> submitting -> submitted 或 failed_submit。
4. failed_submit 不自动重试，等待人工处理。

### 5) 超时与中断
1. review_pending 超时 -> skipped。
2. run/stop 触发硬中断：不再发起后续点击/提交。

---

## 接口定义

### 后端 API 伪代码
```python
# backend/app/api/automation_routes.py

@router.post('/api/automation/session/start')
def start_session(req: StartSessionReq) -> StartSessionResp

@router.post('/api/automation/scan/start')
def start_scan(req: RunReq) -> AckResp

@router.get('/api/automation/tasks')
def list_tasks(status: str | None, school: str | None, page: int, page_size: int) -> TaskListResp

@router.post('/api/automation/tasks/select')
def select_tasks(req: SelectTasksReq) -> AckResp

@router.post('/api/automation/grab/start')
def start_grab(req: BatchReq) -> AckResp

@router.post('/api/automation/solve/start')
def start_solve(req: BatchReq) -> AckResp

@router.post('/api/automation/task/{task_id}/review/save')
def save_review(task_id: str, req: SaveReviewReq) -> TaskResp

@router.post('/api/automation/task/{task_id}/confirm-submit')
def confirm_submit(task_id: str) -> TaskResp

@router.post('/api/automation/run/pause')
def pause_run(req: RunReq) -> AckResp

@router.post('/api/automation/run/resume')
def resume_run(req: RunReq) -> AckResp

@router.post('/api/automation/run/stop')
def stop_run(req: RunReq) -> AckResp  # hard stop

@router.get('/api/automation/logs')
def list_logs(run_id: str, task_id: str | None, limit: int = 200) -> LogListResp
```

### 关键 Schema 伪代码
```python
class AutomationTask(BaseModel):
    task_id: str
    run_id: str
    school_name: str
    topic_title: str
    topic_image_url: str | None
    status: Literal[
      'discovered','selected','grabbed','solving','solve_failed','filled',
      'review_pending','ready_to_submit','submitting','submitted','failed_submit',
      'skipped','paused','stopped'
    ]
    final_markdown: str | None
    analysis_markdown: str | None
    extension_text: str | None
    analysis_edited: str | None
    extension_edited: str | None
    error_code: str | None
    error_message: str | None
    created_at: datetime
    updated_at: datetime
```

```ts
// frontend/src/automation-console/types.ts
export interface TaskItem {
  task_id: string;
  school_name: string;
  topic_title: string;
  status: string;
  analysis_preview?: string;
  extension_preview?: string;
  error_message?: string;
}

export interface RunStatus {
  run_id: string;
  mode: 'headed' | 'headless';
  state: 'idle' | 'running' | 'paused' | 'stopped';
  current_task_id?: string;
}
```

---

## 数据库变更

### 新增表 1：automation_tasks
字段建议：
1. id (PK, auto)
2. task_id (UNIQUE)
3. run_id (INDEX)
4. school_name (INDEX)
5. topic_title
6. topic_image_url
7. status (INDEX)
8. final_markdown (TEXT)
9. analysis_markdown (TEXT)
10. extension_text (TEXT)
11. analysis_edited (TEXT)
12. extension_edited (TEXT)
13. retry_count (INT, default 0)
14. error_code
15. error_message
16. created_at
17. updated_at

### 新增表 2：automation_logs
字段建议：
1. id (PK)
2. run_id (INDEX)
3. task_id (INDEX)
4. school_name
5. step
6. level
7. message
8. payload_summary
9. screenshot_path
10. created_at

---

## 实施步骤 (Task List)
1. [Step 1] 新建 backend/app/automation/ 目录与基础文件骨架。
2. [Step 2] 在 backend/app/automation/models.py 定义 automation_tasks 与 automation_logs 模型。
3. [Step 3] 在 backend/app/automation/schemas.py 定义 API 入参与出参模型。
4. [Step 4] 在 backend/app/automation/state_machine.py 实现状态流转校验器（非法流转抛错）。
5. [Step 5] 在 backend/app/automation/repository.py 实现任务与日志的 CRUD/分页/过滤。
6. [Step 6] 在 backend/app/automation/logging_store.py 实现结构化日志写入（DB + JSONL 可选）。
7. [Step 7] 在 backend/app/automation/browser_worker.py 封装 Playwright 页面动作（登录、切校、接单、识别录入、提交）。
8. [Step 8] 在 backend/app/automation/markdown_renderer.py 实现 Markdown -> 图片渲染能力。
9. [Step 9] 在 backend/app/automation/service.py 编排扫描、接单、解题、复核、提交全流程（串行）。
10. [Step 10] 在 backend/app/api/automation_routes.py 暴露控制台 API。
11. [Step 11] 修改 backend/app/main.py：仅 include_router(automation_router)。
12. [Step 12] 更新 backend/requirements.txt 并补充安装脚本（playwright install chromium）。
13. [Step 13] 新建 frontend/src/automation-console/types.ts 与 api.ts。
14. [Step 14] 新建 frontend/src/automation-console/App.tsx 与基础组件（TaskTable/ReviewPanel/RunControls/LogPanel）。
15. [Step 15] 新建 frontend/src/automation-console/main.tsx 并提供独立入口页面。
16. [Step 16] 联调：扫描候选池 -> 批量接单 -> 解题写入 -> review_pending 编辑 -> 提交。
17. [Step 17] 异常联调：solve_failed、failed_submit、skipped、stop(硬中断)。
18. [Step 18] 验收并输出运行手册。

---

## 每步测试点（原子可测）
1. 状态机单测：非法流转必须报错。
2. 串行保障：任一时刻仅有一个任务处于 solving/submitting。
3. OCR 成功判定：HTTP 200 + code=200 + text 非空。
4. 提交前复核：未确认时不可调用提交动作。
5. 超时策略：review_pending 超时自动 skipped。
6. stop 硬中断：点击 stop 后不再产生新的提交请求。

---

## 研发执行禁令（防止自由发挥）
1. 不得修改 backend/app/agent/* 节点实现。
2. 不得在 frontend/src/App.tsx 继续堆叠新业务。
3. 不得引入并行队列/多线程并发提交。
4. 不得将账号密码、完整 Cookie 写入日志。
5. 未经评审不得新增跨进程中间件（如 Redis/Celery）。

---

## 交付物定义
1. 架构实现代码（后端 automation 子模块 + 新前端控制台）。
2. 数据迁移说明（新增表）。
3. API 文档（OpenAPI + 示例）。
4. 运行手册（headed/headless、人工接管、失败处置）。
5. 验收报告（按 PRD 验收标准逐项打勾）。
