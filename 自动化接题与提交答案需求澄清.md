# 自动化接题与提交答案 PRD v1.0（可开发版）

## 1. 文档目的
将口语化需求收敛为可直接研发实现的产品需求，覆盖：功能范围、业务流程、状态机、异常策略、数据模型、接口合同、验收标准。

## 2. 产品目标
构建一个独立前端控制台，对接自动化执行后端，实现以下能力：
1. 全量学校扫描待解题并落库为候选任务。
2. 用户批量勾选后，按学校闭环接单。
3. 在“解题中”逐题调用现有工作流解题并自动回填。
4. 提交前给用户预览和修改，确认后再提交。
5. 全链路日志可观测，失败可人工接管。

## 3. 范围与非范围
### 3.1 范围（In Scope）
1. 独立新前端（状态看板 + 人工确认 + 批量操作）。
2. 自动化后端（Playwright、任务调度、日志落库、状态机）。
3. 与现有工作流集成（题图输入、`final_answer/final_result`读取）。
4. 答案回填采用“识别录入”图片粘贴链路。

### 3.2 非范围（Out of Scope）
1. 不改造目标平台前端代码。
2. 不实现目标平台接口逆向直调替代页面行为（仅浏览器自动化交互）。
3. 不做多题并行处理（明确禁止并行）。

## 4. 关键业务约束（已确认）
1. 仅账号密码登录。
2. 学校全量遍历。
3. 一次只能处理一道题，绝对禁止并行处理。
4. 提交前必须人工确认，且可修改内容。
5. 可见模式下允许人工接管浏览器。
6. 提交失败显示 failed，等待人工处理（不自动重试提交）。
7. 工作流解题失败时，该题保持在待解题/未提交状态，系统继续处理下一题。
8. 同校闭环：在一个学校接单的题，必须在该学校内完成“解题中”处理后再切换下一学校。
9. 人工确认超时默认行为：skipped。

## 5. 端到端业务流程
### 5.1 阶段 A：扫描与候选池
1. 用户启动“全量扫描”。
2. 系统遍历学校下拉，逐校进入“待解题”。
3. 识别题目并落库为 `discovered`。
4. 前端展示候选池，用户批量勾选，状态改为 `selected`。

### 5.2 阶段 B：按学校批量接单
1. 系统按学校分组处理 `selected` 任务。
2. 切换学校，在“待解题”逐题点击“我会做，抢单答题”。
3. 成功接单后状态改为 `grabbed`，题目进入“解题中”。

### 5.3 阶段 C：逐题解答与回填
1. 进入“解题中”列表（点击状态栏“解题中”）。
2. 一次取 1 题（串行）。
3. 抓题图 -> 调用现有工作流 -> 获取 `final_answer/final_result`。
4. 文本切分：
   - `【正解】` 到 `【考点延伸】` 前：解析片段
   - `【考点延伸】` 后：考点延伸片段
5. 解析片段处理：Markdown 渲染为图片 -> 点击“识别录入” -> 粘贴图片 -> 等待 `imgToText` 成功并回填编辑器。
6. 考点延伸片段处理：直接文本写入“考点衍生”输入框。

### 5.4 阶段 D：提交前复核与提交
1. 自动写入完成后状态置为 `review_pending`。
2. 前端展示“解析预览 + 考点衍生预览”。
3. 用户可编辑并保存修订。
4. 用户点击确认后状态置为 `ready_to_submit`，执行页面提交。
5. 结果：
   - 成功 -> `submitted`
   - 失败 -> `failed_submit`（人工处理）

## 6. 状态机定义
### 6.1 任务状态
1. `discovered`：扫描发现，待用户选择。
2. `selected`：用户已勾选，待接单。
3. `grabbed`：已接单，待解题。
4. `solving`：正在调用工作流解题。
5. `solve_failed`：工作流失败，本题不提交，继续下一题。
6. `filled`：内容已自动写入页面。
7. `review_pending`：等待人工预览/编辑/确认。
8. `ready_to_submit`：用户确认可提交。
9. `submitting`：正在提交。
10. `submitted`：提交成功。
11. `failed_submit`：提交失败，等待人工处理。
12. `skipped`：人工确认超时或人工跳过。
13. `paused`：系统暂停。
14. `stopped`：系统硬中断停止。

### 6.2 合法流转
1. `discovered -> selected -> grabbed -> solving`
2. `solving -> solve_failed` 或 `solving -> filled -> review_pending`
3. `review_pending -> ready_to_submit -> submitting -> submitted`
4. `submitting -> failed_submit`
5. `review_pending -> skipped`
6. 任意运行态 -> `paused` 或 `stopped`

## 7. 人工确认与超时策略
1. 候选池批量确认：超时 `skipped`。
2. 提交前确认：超时 `skipped`。
3. `skipped` 任务不自动提交，可后续手动重新入队。

## 8. 并发与调度策略
1. 全系统单线程串行执行，`max_concurrency = 1`。
2. 单题执行锁：任一时刻仅一个 `solving/submitting` 任务。
3. 学校闭环调度：当前学校存在 `grabbed/review_pending/ready_to_submit/submitting` 任务时，不允许切换学校。

## 9. 浏览器运行模式
1. `headed`（可见模式）：默认模式，用户可实时查看脚本操作。
2. `headless`（无头模式）：可选模式。
3. 可见模式支持人工接管：
   - 接管后系统状态置 `paused`。
   - 用户确认“继续自动化”后恢复。

## 10. 内容映射规则（最终口径）
1. 输入源：工作流返回 `final_answer/final_result`（Markdown）。
2. 锚点：`【考点延伸】`。
3. 解析片段：
   - 从 `【正解】` 到 `【考点延伸】` 前。
   - 若无 `【正解】`，则从文本开头到锚点前。
   - 处理：Markdown 渲染图片 -> 识别录入写入“答案及解析”。
4. 考点延伸片段：
   - 锚点后至文本结尾。
   - 处理：纯文本写入“考点衍生”输入框。

## 11. 已确认 DOM 与接口线索
### 11.1 登录页
1. 账号：`input[type='text'][placeholder='请输入账号'].el-input__inner`
2. 密码：`input[type='password'][placeholder='请输入密码'].el-input__inner`
3. 登录按钮：`button.el-button.el-button--primary.el-button--default > span`（文本：立即登录）

### 11.2 状态与编辑区
1. 状态栏：`div.status_bar`
2. 解题中入口：`div.status_item`（文本：解题中）
3. 识别录入按钮：`button.el-button.el-button--text.el-button--mini > span`（文本：识别录入）
4. 粘贴区：`input[type='text'][placeholder='粘贴答案图片'][readonly='readonly']`
5. 编辑器：`div[id^='w-e-textarea-'][contenteditable='true'][role='textarea']`

### 11.3 关键接口
1. OCR：`POST https://yy.xuejie.cn/admin/openai/imgToText`
2. 请求：`multipart/form-data` + Cookie 会话态
3. 成功判定：HTTP 200 且 JSON `code == 200` 且 `data.res.text` 非空
4. 已观测相关流程接口：`aiTopicList`、`list`、`aiTopicInfo`、`startDevAiTopic`

## 12. 后端 JSON 数据模型（建议实现）
### 12.1 tasks.json（任务主表）
1. `task_id`：字符串，主键。
2. `school_name`：字符串。
3. `topic_title`：字符串。
4. `topic_image_url`：字符串。
5. `status`：枚举（见状态机）。
6. `selected_by_user`：布尔。
7. `final_answer_markdown`：字符串。
8. `analysis_render_image_path`：字符串。
9. `analysis_text_submitted`：字符串。
10. `extension_text_submitted`：字符串。
11. `error_code`：字符串。
12. `error_message`：字符串。
13. `retry_count`：数字。
14. `created_at` / `updated_at`：时间戳。

### 12.2 logs.jsonl（事件日志）
1. `run_id`
2. `task_id`
3. `school_name`
4. `step`
5. `level`（INFO/WARN/ERROR）
6. `message`
7. `payload_summary`（脱敏）
8. `screenshot_path`（可选）
9. `timestamp`

## 13. 后端 API 合同（MVP）
1. `POST /api/session/start`
   - 入参：账号、密码、运行模式(`headed/headless`)
   - 出参：`run_id`、`session_status`
2. `POST /api/scan/start`
   - 入参：`run_id`
   - 出参：扫描任务已启动
3. `GET /api/tasks?status=discovered|selected|grabbed|review_pending|failed_submit|submitted`
4. `POST /api/tasks/select`
   - 入参：`task_ids[]`
5. `POST /api/grab/start`
   - 入参：`task_ids[]`
6. `POST /api/submit/batch/start`
   - 入参：`task_ids[]`
7. `POST /api/task/{task_id}/review/save`
   - 入参：`analysis_text`、`extension_text`
8. `POST /api/task/{task_id}/confirm-submit`
9. `POST /api/run/pause`
10. `POST /api/run/resume`
11. `POST /api/run/stop`（硬中断）
12. `GET /api/logs?run_id=...`

## 14. 异常处理策略
1. 工作流失败：
   - 状态置 `solve_failed`
   - 该题不提交
   - 继续下一题
2. 提交失败：
   - 状态置 `failed_submit`
   - 前端标红并等待人工处理
3. OCR 失败：
   - 记录错误并置 `review_pending` 或 `solve_failed`（按错误类型）
4. 页面元素缺失/超时：
   - 记录截图+日志
   - 当前题置失败态
   - 队列继续下一题

## 15. 停止与中断语义（强约束）
1. `stop` 为硬中断：
   - 立即停止后续自动化步骤
   - 不再触发新的点击、提交或工作流调用
2. 正在执行中的步骤：
   - 若可取消则立即取消
   - 若不可取消则执行最小安全收尾后退出
3. 中断后所有未完成任务保留原状态，待人工恢复。

## 16. 安全与合规
1. 账号密码仅会话内存保存，默认不落盘。
2. 日志脱敏，禁止写入明文密码与完整 Cookie。
3. 导出日志默认屏蔽敏感头与鉴权字段。

## 17. 验收标准（UAT）
1. 能完成“扫描 -> 候选池确认 -> 接单 -> 解题 -> 预览编辑 -> 提交”全链路。
2. 全程无并行处理（日志可证明串行执行）。
3. 提交前必经人工确认，且可编辑。
4. 提交失败正确进入 `failed_submit`，不自动提交重试。
5. 工作流失败正确进入 `solve_failed`，并继续处理下一题。
6. 可见模式下可实时观察脚本操作，并可人工接管。
7. `stop` 可触发硬中断，不会继续提交后续题目。

## 18. 仍需你最终拍板的仅剩事项
1. `review_pending` 超时时长（建议 10 分钟，超时 `skipped`）。
2. `solve_failed` 是否允许一键“重新入队到 selected”。
3. `failed_submit` 是否允许一键“回到 ready_to_submit”。
